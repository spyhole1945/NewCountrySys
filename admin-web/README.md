# RuralSync Admin Web Dashboard

乡村同行管理后台 - 基于 Vue 3 + Vite + Element Plus 的现代化管理系统

## 项目概述

这是一个功能完整的管理后台系统,用于管理乡村同行平台的问题上报、内容审核和用户管理。

### 核心特性

✅ **现代化技术栈**
- Vue 3 (Composition API)
- Vite (快速构建)
- Element Plus (UI组件库)
- Vue Router (路由管理)
- Pinia (状态管理)
- Axios (HTTP客户端)
- ECharts (数据可视化)

✅ **完整功能模块**
- 🔐 **登录认证** - JWT token管理
- 📊 **仪表盘** - 数据统计和图表展示
- ⚠️ **问题管理** - 问题列表、详情、状态更新
- 📝 **内容审核** - 社区动态和集市商品审核
- 👥 **用户管理** - 用户列表、角色管理、状态控制

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问: http://localhost:5173

### 3. 登录系统

默认管理员账号:
- 用户名: `admin`
- 密码: `admin123`

## 项目结构

```
admin-web/
├── src/
│   ├── api/              # API接口模块
│   │   ├── auth.js      # 认证接口
│   │   ├── issue.js     # 问题管理接口
│   │   ├── user.js      # 用户管理接口
│   │   └── content.js   # 内容审核接口
│   ├── layouts/          # 布局组件
│   │   └── AdminLayout.vue  # 管理后台布局
│   ├── router/           # 路由配置
│   │   └── index.js     # 路由定义和守卫
│   ├── stores/           # Pinia状态管理
│   │   └── auth.js      # 认证状态
│   ├── utils/            # 工具函数
│   │   ├── request.js   # Axios封装
│   │   └── constants.js # 常量定义
│   ├── views/            # 页面组件
│   │   ├── Login.vue           # 登录页
│   │   ├── Dashboard.vue       # 仪表盘
│   │   ├── IssueManagement.vue # 问题管理
│   │   ├── ContentAudit.vue    # 内容审核
│   │   └── UserManagement.vue  # 用户管理
│   ├── App.vue
│   └── main.js
├── package.json
└── vite.config.js
```

## 功能说明

### 🔐 登录认证

- 表单验证
- JWT token自动注入
- 401自动跳转登录
- Token持久化存储

### 📊 仪表盘

- **统计卡片**: 问题总数、待处理、处理中、已完成
- **折线图**: 月度问题上报趋势
- **饼图**: 问题状态分布
- 响应式布局

### ⚠️ 问题管理

- 分页列表展示
- 状态筛选
- 问题详情查看
- 状态更新(待处理→处理中→已完成)
- 处理意见录入

### 📝 内容审核

- Tab切换(社区动态/集市商品)
- 审核通过/删除操作
- 确认对话框
- 分页支持

### 👥 用户管理

- 用户列表展示
- 角色筛选和关键词搜索
- 角色修改(村民/村务人员/管理员)
- 用户状态切换(启用/禁用)

## API对接

后端API地址配置在 `src/utils/request.js`:

```javascript
baseURL: 'http://localhost:8080'
```

### API端点

- `POST /api/auth/login` - 登录
- `GET /api/admin/issues` - 问题列表
- `PUT /api/admin/issues/{id}/status` - 更新问题状态
- `GET /api/admin/issues/stats` - 统计数据
- `GET /api/admin/users` - 用户列表
- `PUT /api/admin/users/{id}/role` - 更新用户角色
- `GET /api/admin/content/posts` - 社区动态列表
- `GET /api/admin/content/market-items` - 集市商品列表
- `PUT /api/admin/content/{type}/{id}/approve` - 审核通过
- `DELETE /api/admin/content/{type}/{id}` - 删除内容

## 构建生产版本

```bash
npm run build
```

构建产物将生成在 `dist/` 目录。

## 技术亮点

1. **Composition API**: 使用Vue 3最新特性
2. **自动路由守卫**: 未登录自动跳转
3. **统一错误处理**: Axios拦截器处理所有错误
4. **响应式设计**: 适配不同屏幕尺寸
5. **ECharts集成**: 数据可视化展示
6. **Element Plus**: 企业级UI组件

## 开发注意事项

1. **后端服务**: 确保后端服务运行在 `http://localhost:8080`
2. **Token管理**: Token存储在localStorage,刷新页面不会丢失
3. **Mock数据**: 当前后端使用mock数据,实际使用需连接真实数据库

## 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge

## 后续优化

- [ ] 实时通知功能
- [ ] 数据导出(Excel)
- [ ] 高级筛选和搜索
- [ ] 批量操作
- [ ] 操作日志记录
- [ ] 深色模式支持

## License

MIT
